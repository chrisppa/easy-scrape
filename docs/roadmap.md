Roadmap

Near Term

- Retry/backoff middleware and per-domain rate limiting
- JSONLines/CSV export pipelines and `--output` CLI support
- HTTP caching and robots.txt handling
- CLI flags to register middlewares/pipelines
- Central settings with CLI overrides

Future

- Browser pooling for Playwright requests
- Auto-throttle and polite crawling defaults
- Project template command (`easy-scrape startproject`)
